<b>Active:</b>
<% if session[:equipment_piece_list_id] %>
  <% eq_list = EquipmentPieceList.find(session[:equipment_piece_list_id]) %>
  <% if eq_list %>
    <%= link_to eq_list.name, eq_list %>
  <% end %>
<% end %>
