<% 
  if params[:id_only]
    ret = "#{@equipment_piece.id}"
  else
    ret = "tell %2 #{@equipment_piece.name} - #{@equipment_piece.tp_value} tps | #{slot_line(@equipment_piece)}\n" 
    ret = ret + "tell %2 #{simple_stat_line(@equipment_piece)}\n"
  
    price = @equipment_piece.rufrin_price
    if price and price > 0 
      ret = ret + "tell %2 Rufrin last offered #{price} for this item\n"
    end
  end
%>
<%= ret %>
